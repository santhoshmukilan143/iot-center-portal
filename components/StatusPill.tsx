const map:any={pending:['Pending','bg-amber-50 text-amber-700'],in_progress:['In Progress','bg-blue-50 text-blue-700'],submitted:['Submitted','bg-violet-50 text-violet-700'],reviewed:['Reviewed','bg-emerald-50 text-emerald-700'],overdue:['Overdue','bg-red-50 text-red-700']};export default function StatusPill({status}:{status:keyof typeof map}){const [t,c]=map[status]||map.pending;return <span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${c}`}>{t}</span>}
